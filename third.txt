我一直进行尝试push本地的东西到远程上去，但是一直没有成功，就像在原地踏步走一样，我按知乎上的方法git push origin master
报了很多错误，我直接push也会报错。后来慢慢尝试直接git push，竟然上去了，我很开心。但是git push origin master 还是会报
错，后来看评论才知道，应该是版本的问题，把master改成main就好了，或者直接push，有一定的几率成功。还有就是网上搜索的
的时候一定要看最新的解答，因为一前的可能会因为版本更替解决不了问题。现在尝试第三个，git push origin main 会怎样。